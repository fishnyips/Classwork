;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname labinterface04) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Lab 04, Question 2

(define (in-subset-1? x)
  ...)

(define (in-subset-2? x)
  ...)

(define (in-subset-3? x)
  ...)

;; Lab 04, Question 3

(define (acceptable-bid? current-high next-bid)
  ...)

;; Lab 04, Question 4

(define (new-string original add-on position)
  ...)

;; Lab 04, Question 5

(define (connect? string1 string2)
  ...)
