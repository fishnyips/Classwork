;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-abbr-reader.ss" "lang")((modname l07-interface) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Lab 07, Question 1
(define (is-prime? n)
  ...)

;; Lab 07, Question 2

(define (repeat n s)
  ...)

;; Lab 07, Question 3

(define (exponent base expt)
  ...)

;; Lab 07, Question 4

(define (largest-prime bottom top)
  ...)

;; Lab 07, Question 5

(define (total-price-list lol)
  ...)

;; Lab 07, Question 6

(define (remove-al key al)
  ...)

;; Lab 07, Question 7

(define (exponent-without-mult base expt)
  ...)