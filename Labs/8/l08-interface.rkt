;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-abbr-reader.ss" "lang")((modname l08-interface) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Lab 08, Question 1
(define (extract-values keys assolist)
  ...)

;; Lab 08, Question 2
(define (extract-bad bad-part whole-string)
  ...)

;; Lab 08, Question 3
(define (subseq-string pattern target)
  ...)

;; Lab 08, Question 4
(define (clicker-grades corr-answers stu-answers)
  ...)

;; Lab 08, Question 5
(define (clicker-grades-absent corr-answers stu-answers)
  ...)