;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname lab-interface06) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Lab 06, Question 2

(define (switch-case str)
  ...)

;; Lab 06, Question 3

(define (list-pos nelos elt)
  ...)

;; Lab 06, Question 4

(define (differences nelon)
  ...)

;; Lab 06, Question 5

(define (average-filter log)
  ...)
