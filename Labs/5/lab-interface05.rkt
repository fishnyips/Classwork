;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname lab-interface05) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Lab 05, Question 2

(define (count-even-strings los)
  ...)

;; Lab 05, Question 3

(define (longest-string-length los)
  ...)

;; Lab 05, Question 4

(define (or-with-lists? lob)
  ...)

;; Lab 05, Question 5

(define (strings-with-prefix los pre)
  ...)

;; Lab 05, Question 6

(define (next-list alist item)
  ...)
