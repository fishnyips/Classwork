;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname a07-interface) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Assignment 07 Interface

;; Question 1 - copy into a07q1.rkt and replace ... with your solution
(define (vacations destinations prices budget)
  ...)


;; Question 2 - copy into a07q2.rkt and replace ... with your solution
(define (list-of-lists L1 L2)
  ...)

;; Question 3 - copy into a07q3.rkt and replace ... with your solution
(define (password alos aloi)
  ...)