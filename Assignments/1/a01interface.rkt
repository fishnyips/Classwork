;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname a01interface) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #t)))
;; Question 1 - copy into a01q1.rkt and replace ... with your solution
(define (pink-ties-needed a b t)
  ...)

;; Question 2 - copy into a01q2.rkt and replace ... with your solution
(define (wind-chill t-air v)
  ...)

;; Question 3 - copy all three into a01q3.rkt and replace ... with your solutions
(define (weighted-avg-final m)
  ...)

(define (calculation-final a m p)
  ...)

(define (required-final a m p)
  ...)