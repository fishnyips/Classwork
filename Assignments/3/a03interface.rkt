;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname a03-interface.rkt) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Assignment 03 Interface

;; Question 1  - copy into a03q1.rkt and replace ... with your solution
(define (quadrant x y)
  ...)


;; Question 2  - copy into a03q2.rkt and replace ... with your solution
(define (strict-parity-seq? n1 n2 n3 n4)
  ...)

;; Question 3  - copy into a03q3.rkt and replace ... with your solution
(define (same? a b c)
  ...)

(define (tic-tac-toe-win s1 s2 s3 s4 s5 s6 s7 s8 s9)
  ...)